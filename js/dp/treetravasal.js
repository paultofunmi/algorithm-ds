// BST
// In a balance search tree, the max diff btw minHeight and maxHeight is 1;
// DFS as types
// Pre-order: Root Left Right (Explore root before the leaves)
// Post-order: Left rIGHT rOOT (Explore leaf nodes before root)
// In-order: L Root Right (lEFT Most node and end at right most node)
// lEVEL ORDER

class Node {
  constructor(data, left = null, right = null) {
    this.data = data;
    this.left = left;
    this.right = right;
  }
}

class BST {}



Login 
Register
Dashboard 


Unit tests
Git hooks - runs unit tests and acceptance tests or fails otherwise

Redis Cache

